//COMPARE LENGTH

let firstList = [1, 2, 3];
let secondList = [4, 5];

if (secondList.length > firstList.length) {
  console.log('secondList is longer');
} else if (secondList.length === firstList.length) {
  console.log('the lengths of the lists are equal');
} else console.log('firstList is the longer one');
